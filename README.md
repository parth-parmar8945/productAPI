A simple and clean Product Listing Web App where products are fetched dynamically
from an API (FakeStore API or your custom backend). The application displays product 
images, titles, descriptions, and prices, along with a View Details button for each item.
